/**
 * Load fixtures (execs + sample statements) into a hosted Supabase project.
 * Real implementation lands in Phase 1; placeholder for now.
 */
import { logger } from "../src/lib/logger";

function main(): void {
  logger.info("db:seed is not wired yet (Phase 1). No fixtures loaded.");
}

main();
